{
  "name": "habit-tracker",
  "version": "1.0.0",
  "description": "A simple habit tracking application",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^1.1.0",
    "happy-dom": "^12.10.3",
    "vitest": "^1.1.0"
  }
}
